<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detailed</title>
    <link rel="stylesheet" href="/css/detail.css">
    <script>
        function confirmDelete(){
            return confirm("Are you sure to delete list.");
        } 
    </script>
</head> -->

<% layout('/layouts/boilerplate') -%>

<body>
  <div class="container py-2">
    <div class="row justify-content-center">
      <div class="col-md-10">
        
        <h2 class="card-title"><%= list.title %></h2>
        
        <!-- Listing Card -->
        <div class="card shadow-sm">
          <img src="<%= list.image.url %>" class="card-img-top show-img" alt="listing image">

          <div class="card-body">

            <p class="text-muted m-2">
              Location: <strong><%= list.location %>, <%= list.country %></strong>
            </p>

            <p><%= list.description %></p>

            <hr>

            <h4 class="text-success">&#8377; <%= list.price.toLocaleString("en-IN") %> / night</h4>

            <hr>

            <!-- Actions -->
            <div class="d-flex justify-content-between mt-4">
              <a href="/lists/<%= list._id %>/edit" class="btn btn-outline-primary">Edit</a>

              <form method="POST" action="/lists/<%= list._id %>?_method=DELETE" onsubmit="return confirmDelete()">
                <button class="btn btn-outline-danger">Delete</button>
              </form>
            </div>
          </div>
        </div>

      </div>
    </div>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

  <!-- Optional: Delete confirmation -->
  <script>
    function confirmDelete() {
      return confirm("Are you sure you want to delete this listing?");
    }
  </script>

</body>
</html>






    <!-- <body>
        <div class="content">
             <div class="left">
            <h1><%= list.title %></h1>
            <img src="<%= list.image.url %>">
            </div> 

            <div class="card">
                <img src="<%= list.image.url %>" class="card-img-top show-img" alt="listing image">
                <div class="card-body">
                    <p class="card-text">
                        <h3>
                            <%= list.title %>
                        </h3>
                        <p>
                            <%= list.description %>
                        </p>
                        <p>Location: <b>
                                <%= list.location %>, <%= list.country %>
                            </b></p>
                        <hr>
                        <h4>&#8377; <%= list.price.toLocaleString("en-IN") %>/day/person</h4>
                        <hr>
                    </p>
                </div>
            </div>


            <div class="right">
                
            </div>
            <a class="update-link btn btn-success" href="/lists/<%= list._id %>/edit">Edit</a>


            <form method="POST" action="/lists/<%= list._id %>?_method=DELETE" onsubmit="return confirmDelete()">
                <button class="delete-link btn btn-danger mt-3">Delete</button>
            </form>
        </div>
        </div>
    </body> -->

    </html>